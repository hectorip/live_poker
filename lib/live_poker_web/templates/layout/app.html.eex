<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Focus Tools</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
    <%= render_existing view_module(@conn), "scripts."  <> view_template(@conn), assigns %>
  </head>
  <body>
    <header class="bg-gray-300">
      <nav class="container mx-auto flex items-center justify-between flex-wrap p-3 bg-gray-300">
        <div class="flex items-center flex-shrink-0 text-white">
          <span class="font-semibold tracking-tight ">
            <img class="logo" src="<%= Routes.static_path(@conn, "/images/logo_transparent.png") %>" alt="Logo Focus Tools"/>
          </span>
        </div>
        <div class="block lg:hidden">
          <button class="flex items-center px-3 py-2 border rounded text-indigo-600 border-indigo-400 hover:text-white hover:border-white">
            <svg class="fill-current h-3 w-3" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><title>Menu</title><path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z"/></svg>
          </button>
        </div>
        <div class="w-full block lg:w-auto lg:flex lg:flex-row lg:flex-end">
          <div class="float-right">
            <a href="#" class="inline-block text-lg px-4 py-2 rounded text-indigo-700 hover:border-transparent hover:text-indigo-500 hover:bg-white mt-4 lg:mt-0 right-0">
              Información
            </a>
            <a href="#" class="inline-block text-lg px-4 py-2 rounded text-indigo-700 hover:border-transparent hover:text-indigo-500 hover:bg-white mt-4 lg:mt-0 right-0">
              Registro
            </a>
            <a href="#" class="inline-block text-lg px-4 py-2 rounded text-indigo-700 hover:border-transparent hover:text-indigo-500 hover:bg-white mt-4 lg:mt-0 right-0">
              Login
            </a>
          </div>
        </div>
      </nav>
    </header>
    <main role="main" class="container mx-auto p-6">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= @inner_content %>
    </main>
  </body>
</html>
